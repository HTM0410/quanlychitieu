[build]
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"
  NODE_ENV = "production"
  NPM_FLAGS = "--legacy-peer-deps --prefer-offline"
  CI = "true"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200 